#include <iostream>
using namespace std;
int main(){
	//example of class Extension
	/* class FrozenPizza : public FrozenFood*/
	
	//example 0f overiding member functions using (virtual)
	/* virtual void BakeChemistry(); */
	/* void BakeChemistry(); */
	
	//example of abstraction, must have at least one virtual function with no code
	/* virtual void BakeChemistry() = 0;*/
	
	// we use :: to dig in deeper from a class to class or from namespace to class
	return 0;
}
